# TaxoConserv Web Interface - Minimal Requirements
# For running only the web application

# Include base requirements
-r requirements.txt

# Web interface essentials (already included in main requirements)
# streamlit>=1.25.0
# plotly>=5.15.0

# Additional web-specific packages
watchdog>=3.0.0      # For auto-reload during development
streamlit-aggrid>=0.3.4  # For enhanced data tables (optional)
streamlit-plotly-events>=0.0.6  # For plot interactions (optional)

# Performance monitoring for web app
psutil>=5.9.0        # For system resource monitoring
memory-profiler>=0.61.0  # For memory usage tracking
